export type ITheme = {
  main: string;
  secondary: string;
  text: string;
  hover: string;
  active: string;
  letterText: string;
  letterHeadText: string;
  separator: string;
};

export type IThemeType = "dark" | "light";
export type IThemeSize = "small" | "big";
