export type ITheme = {
  main: string;
  secondary: string;
  text: string;
  hover: string;
  active: string;
  letterText: string;
  letterHeadText: string;
  separator: string;
  downloadGradient: string;
  popupGradient: string;
};

export type IThemeType = "dark" | "light";
